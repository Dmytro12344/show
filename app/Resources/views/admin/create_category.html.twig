{% extends 'admin/index.html.twig' %}

{% block body %}
    <div class="container-fluid mt-5">
{{ form_start(form) }}
    <div class="row justify-content-center align-items-center">
        <div class="col-6">
            <div class="">
                {{ form_label(form.name) }}
                {{ form_widget(form.name)}}
            </div>

            <div class="mt-3">
                {{ form_widget(form.save)}}
            </div>
        </div>
    </div>
{{ form_end(form) }}

<a href="{{ path('show_admin_category') }}" class="btn btn-success mt-3">{{ 'category.button.back_to_cat' | trans }}</a>
    </div>
{% endblock %}